import type { Metadata } from 'next'
import { GivingPage } from '@/components/institutional-pages'

export const metadata: Metadata = { title: 'Giving Architecture', description: 'Verified giving channels, asset controls and charitable stewardship at Rein Protocol Foundation.' }
export default GivingPage
